methods:
  post: [email, password, repassword, firstname, lastname, gender, dob]

names:
  email:
    required:     true
    required_msg: E-posta adresinizi girmeniz gerekli.
    validators:   [emailValidator, uniqueEmailValidator]

  password:
    required:     false
    required_msg: Þifreniz gerekli.

  repassword:
    required:     false
    required_msg: Þifreniz gerekli.
    validators:   [passwordValidator]

  firstname:
    required:     false
    validators:   nameValidator

  lastname:
    required:     false
    validators:   nameValidator
  
  gender:
    required:     true
    required_msg: Cinsiyetinizi seçmeniz gerekli.
    
  dob:
    required:     true
    required_msg: Doðum tarihinizi girmeniz gerekli.
    validators:   dobValidator

emailValidator:
  class:            sfEmailValidator
  param:
    email_error:    check your email
    
uniqueEmailValidator:
  class:            sfPropelUniqueValidator
  param:
      class:        User
      column:       email
      unique_error: This email already exists. Please choose another one.

passwordValidator:
  class:            sfCompareValidator
  param:
    check:          password
    compare_error:  passwords do not match

nameValidator:
  class:            sfStringValidator
  param:
    min:            2
    min_error:      2 characters min
    max:            100
    max_error:      100 characters max

dobValidator:
  class:            myDobValidator
  param:
    date_error:     not valid date
